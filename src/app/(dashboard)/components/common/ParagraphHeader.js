const ParagraphHeader = ({content}) => {
    const headerText = content
    return (
        <p className="text-[23px] text-[#16252C] font mb-2">{headerText}</p>
    );
}

export default ParagraphHeader;